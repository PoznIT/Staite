services:
  staite-mcp:
    build:
      context: ../
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./config.yml:/app/config.yml:ro
    restart: unless-stopped
  chroma:
    image: chromadb/chroma:latest
    volumes:
      - chroma_data:/chroma/chroma
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v2/heartbeat"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
  ollama:
    image: ollama/ollama:latest
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped
    entrypoint: [ "/bin/sh", "-c" ]
    command: [ "ollama serve & sleep 5 && ollama pull nomic-embed-text && wait" ]
    healthcheck:
      test: [ "CMD-SHELL", "ollama list || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: staite
      POSTGRES_USER: staite
      POSTGRES_PASSWORD: staite
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U staite -d staite" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  chroma_data:
  ollama_data:
  postgres_data:
